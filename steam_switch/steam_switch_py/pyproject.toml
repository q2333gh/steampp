[project]
name = "steam-switch-py"
version = "0.1.0"
description = "Windows-only Steam account switch CLI"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
steam-switch = "steam_switch_py.cli:main"
steam-switch-gui = "steam_switch_py.gui:main"

[build-system]
requires = ["hatchling>=1.24.0"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = []
